# Test/dev dependencies (kept separate from runtime requirements.txt).
# The unit suite covers pure logic (aggregator voting, quality scorer, KNN,
# keyword, circuit breaker) and needs no DB/Redis/Ollama — so only pytest.
pytest>=8.0
pytest-cov>=5.0
